

<h1><%= @post.appointment %></h1>

	<p><%= @post.about %></p>

	<p><%= @post.time %></p>
Submitted <%= time_ago_in_words(@post.created_at) %> ago

<br>
| <%= link_to 'Edit', edit_post_path(@post) %>
	| <%= link_to 'Delete', post_path(@post), method: :delete, data: {confirm:'Are you sure you want to deleted appointment?'} %>